# 2026년 약관 KB 전면 재구축 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 확보한 2026년 6개사 약관 PDF 8개를 유일한 원본으로 삼아 KB(`backend/data/app.db`)의 담보·조항과 그 파생 데이터를 전부 다시 만든다.

**Architecture:** 기존 KB와 사용자 데이터를 지우고(`reset_kb.py`), 새 PDF를 보험사별 청크로 정독해 `seed_<insurer>_2026_<chunk>.py`를 새로 쓴 뒤, 조항에서 파생되는 6종 매핑을 다시 만든다. 모든 조항이 원본 PDF 텍스트의 부분 문자열인지 검사하는 `verify_clause_grounding.py`를 먼저 만들어, 정독 결과를 넣는 즉시 검증한다.

**Tech Stack:** Python 3.13, SQLAlchemy + SQLite, pdfplumber 0.11.9, pytest 8.3.3

**Spec:** `docs/superpowers/specs/2026-08-18-terms-rebuild-2026-design.md`

## Global Constraints

- 모든 `Clause.text`는 pdfplumber `extract_text()` 원문 그대로다. 의역·재구성·다른 보험사 문구 재사용 금지. 표 레이아웃 때문에 열 라벨이 문장 중간에 끼어든 경우만 그 라벨을 제거한다.
- `ClauseTerm.raw_text`, `DocRequirement.anchor_phrase`, `OverlapRule.anchor_phrase`, `ClauseStandardMap.anchor_phrase_insurer`는 반드시 대응 `Clause.text`의 부분 문자열이다. `app.services.kb_seed_common.raw_text_is_grounded()`로 검증 후에만 행을 만든다.
- `CoverageStd` 기존 28개 코드를 재사용한다. 지급 구조가 실제로 다른 담보만 새 `std_code`를 만든다(영문 대문자 스네이크, `is_base=False`).
- `clause_type`은 `보장정의/면책/제한/조건/서류/공통` 중 하나.
- `ClauseIncidentMap.relevance`는 `직접/조건부/면책` 중 하나. `제한`은 레거시 값이므로 새로 만들지 않는다.
- `clause_type='서류'` 조항은 `ClauseIncidentMap`에 매핑하지 않는다(`CoverageDocMap` 경로로 소비된다).
- 8개 L1(INJ/ILL/PROP/LIA/TRV/CHG/EMG/SPC) 어디에도 안 맞으면 억지로 끼우지 말고 `needs_review=True`인 새 L2를 만들어 매핑한다. 조용히 빠뜨리지 않는다.
- 추출이 지저분해 정확히 못 뽑은 부분은 추측하지 않고 시드 스크립트 docstring에 "건너뜀 — 이유"로 남긴다. 읽고 무관하다고 판단한 특약은 "확인함, 무관"으로 남긴다.
- `page_ref` 표기: 단일 파일 보험사는 `p.123`, 카카오는 파일 접두사를 붙여 `K1 p.198` / `K2 p.198` / `K3 p.201`.
- 출처 기록: `source_type='OFFICIAL_ISSUED_FILE'`, `source_url=None`, `downloaded_at=2026-08-18`, `verification_status='VERIFIED_ISSUED_FILE'`, `ranking_eligible=true`.
- Windows 콘솔 인코딩 때문에 `print()`에 em-dash(—) 같은 특수문자를 쓰지 않는다. 괄호나 하이픈을 쓴다.
- 시드 스크립트는 idempotent한 `run()` 함수 하나를 노출한다: `SessionLocal()` 열고, 같은 텍스트가 이미 있으면 건너뛰고, `db.commit()` → `db.close()`.

---

### Task 1: 원본 PDF 이관과 전체 텍스트 캐시

새 PDF를 gitignore 대상 경로로 옮기고, 이후 모든 정독 작업이 읽을 텍스트 캐시를 만든다. 저장소 루트의 `6개 보험사 약관/`은 gitignore에 걸리지 않아 커밋되면 재배포 금지 정책을 위반하므로 반드시 없앤다.

**Files:**
- Create: `backend/data/raw_pdfs/` 아래 8개 PDF (이름 정규화)
- Create: `backend/scripts/extract_raw_pdf_text.py`
- Create: `backend/data/raw_pdfs/source_files.json`
- Delete: `6개 보험사 약관/` (이동 후)

**Interfaces:**
- Produces: `backend/data/raw_pdfs/source_files.json` — 보험사 코드별 파일 목록·SHA-256·쪽수·`page_ref` 접두사. Task 3의 `verify_clause_grounding.py`와 Task 17의 매니페스트 갱신이 이 파일을 읽는다.
- Produces: `backend/data/processed/<stem>_full_text.txt` — `===PAGE n===` 마커로 페이지를 구분한 전체 텍스트.

- [ ] **Step 1: 파일 이동과 이름 정규화**

```bash
cd "C:/Users/User/Desktop/경민/코딩/finance_challenge"
mkdir -p backend/data/raw_pdfs backend/data/processed
mv "6개 보험사 약관/삼성화재.pdf"   backend/data/raw_pdfs/samsung_overseas_2026.pdf
mv "6개 보험사 약관/현대.pdf"       backend/data/raw_pdfs/hyundai_overseas_8403-0000-20260606.pdf
mv "6개 보험사 약관/메리츠.pdf"     backend/data/raw_pdfs/meritz_overseas_2607A.pdf
mv "6개 보험사 약관/kb.pdf"         backend/data/raw_pdfs/kb_overseas_26-15505-1.pdf
mv "6개 보험사 약관/db.pdf"         backend/data/raw_pdfs/db_overseas_promi1_2026.pdf
mv "6개 보험사 약관/카카오 1.pdf"   backend/data/raw_pdfs/kakaopay_overseas_2026-0199_together1.pdf
mv "6개 보험사 약관/카카오 2.pdf"   backend/data/raw_pdfs/kakaopay_overseas_2026-0199_together2.pdf
mv "6개 보험사 약관/카카오 3.pdf"   backend/data/raw_pdfs/kakaopay_overseas_2026-0199_standard.pdf
rmdir "6개 보험사 약관"
```

- [ ] **Step 2: 이동 확인 — 원본 해시가 보존됐는지 대조**

Run:
```bash
cd backend/data/raw_pdfs && sha256sum *.pdf
```
Expected: 아래 8개 해시가 그대로 나온다(파일이 손상 없이 옮겨졌다는 확인).

```
95fc1ac011933d8b329eeee3698cfd8adffa0540bf353b0e90dca446156406b7  samsung_overseas_2026.pdf
19fcd2966b4d17fdca41bd8edfdfff028a4de3ba38ed3574e83406262ec3c878  hyundai_overseas_8403-0000-20260606.pdf
f58406b6496f1031da5f5c870e73c65b7b85ef06d16e2b04448b53d21b61dbdd  meritz_overseas_2607A.pdf
49a5ccecdb5cffe5bcf730efedf13322e0719cff8261aebcf47b724cc064e573  kb_overseas_26-15505-1.pdf
151c57ec603cad5b5d5dcc4128468e2ac9102d45d842554f1d98fbbecbf3e008  db_overseas_promi1_2026.pdf
f558fafbd3439e91525bea49cb46299d43afcf667deb20c5cf505edde997d16b  kakaopay_overseas_2026-0199_together1.pdf
a4b41ae4d606121188a838e6ebc7838dc579d6324c433253b7c394738866d19a  kakaopay_overseas_2026-0199_together2.pdf
3ed801afa9c8e930b01aed97f4ba8d67823d9ee7d5e4d2d533a3a80f67b4009c  kakaopay_overseas_2026-0199_standard.pdf
```

- [ ] **Step 3: 추출 스크립트 작성**

`backend/scripts/extract_raw_pdf_text.py`:

```python
"""원본 약관 PDF의 전체 텍스트를 data/processed/에 캐시한다.

정독 작업은 pdfplumber로 매번 PDF를 여는 대신 이 캐시를 읽는다(같은 파일을 수십 번
여는 비용을 줄이고, Clause.text가 어느 추출본에서 나왔는지 고정한다).
scripts/verify_clause_grounding.py가 같은 캐시로 대조하므로, 캐시를 다시 만들면
검증 기준도 함께 갱신된다.

Run from ``backend``::

    python -m scripts.extract_raw_pdf_text
"""
from __future__ import annotations

import hashlib
import json
from pathlib import Path

import pdfplumber

BACKEND_DIR = Path(__file__).resolve().parents[1]
RAW_DIR = BACKEND_DIR / "data" / "raw_pdfs"
OUT_DIR = BACKEND_DIR / "data" / "processed"
CATALOG = RAW_DIR / "source_files.json"

# 보험사 코드 -> (page_ref 접두사, 파일명). 접두사가 None이면 page_ref에 접두사를 쓰지 않는다.
FILES: dict[str, list[tuple[str | None, str]]] = {
    "SAMSUNG": [(None, "samsung_overseas_2026.pdf")],
    "HYUNDAI": [(None, "hyundai_overseas_8403-0000-20260606.pdf")],
    "MERITZ": [(None, "meritz_overseas_2607A.pdf")],
    "KB": [(None, "kb_overseas_26-15505-1.pdf")],
    "DB": [(None, "db_overseas_promi1_2026.pdf")],
    "KAKAOPAY": [
        ("K1", "kakaopay_overseas_2026-0199_together1.pdf"),
        ("K2", "kakaopay_overseas_2026-0199_together2.pdf"),
        ("K3", "kakaopay_overseas_2026-0199_standard.pdf"),
    ],
}


def sha256_of(path: Path) -> str:
    digest = hashlib.sha256()
    with path.open("rb") as handle:
        for block in iter(lambda: handle.read(1 << 20), b""):
            digest.update(block)
    return digest.hexdigest()


def extract(path: Path, destination: Path) -> int:
    parts = []
    with pdfplumber.open(path) as pdf:
        for index, page in enumerate(pdf.pages, start=1):
            parts.append(f"\n\n===PAGE {index}===\n" + (page.extract_text() or ""))
    destination.write_text("".join(parts), encoding="utf-8")
    return len(parts)


def run() -> dict:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    catalog: dict[str, list[dict]] = {}
    for insurer, entries in FILES.items():
        rows = []
        for prefix, filename in entries:
            pdf_path = RAW_DIR / filename
            if not pdf_path.exists():
                raise FileNotFoundError(f"raw PDF is missing: {pdf_path}")
            text_path = OUT_DIR / f"{pdf_path.stem}_full_text.txt"
            pages = extract(pdf_path, text_path)
            rows.append(
                {
                    "page_ref_prefix": prefix,
                    "pdf": filename,
                    "text": text_path.name,
                    "pages": pages,
                    "sha256": sha256_of(pdf_path),
                }
            )
            print(f"[{insurer}] {filename} -> {text_path.name} ({pages} pages)")
        catalog[insurer] = rows
    CATALOG.write_text(json.dumps(catalog, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"catalog written: {CATALOG}")
    return catalog


if __name__ == "__main__":
    run()
```

- [ ] **Step 4: 실행하고 쪽수를 확인**

Run:
```bash
cd backend && python -m scripts.extract_raw_pdf_text
```
Expected: 삼성 307, 현대 162, 메리츠 244, KB 326, DB 223, 카카오 324/324/331쪽. `source_files.json` 생성.

- [ ] **Step 5: 커밋**

`data/raw_pdfs/`와 `data/processed/`는 gitignore 대상이므로 스크립트만 커밋된다. `source_files.json`도 무시 대상 경로 안에 있으니 커밋되지 않는다 — 의도된 동작이다(파일 목록에 해시가 들어 있고, 커밋 대상 기록은 `dataset_manifest.json`이다).

```bash
cd "C:/Users/User/Desktop/경민/코딩/finance_challenge"
git add backend/scripts/extract_raw_pdf_text.py
git commit -m "2026년 약관 원본 텍스트 추출 스크립트 추가"
```

---

### Task 2: 랭킹 게이트에 VERIFIED_ISSUED_FILE 추가

공개 URL이 없는 보험사 공식 발행 파일을 추천 순위 대상으로 허용한다. 이 상태를 추가하지 않으면 6개사 전부 순위에서 빠진다.

**Files:**
- Modify: `backend/app/services/kb_provenance.py:12`
- Modify: `backend/scripts/validate_kb.py:31-38`
- Test: `backend/tests/test_kb_provenance.py`

**Interfaces:**
- Produces: `verification_status='VERIFIED_ISSUED_FILE'`가 `ranking_eligible=true`로 인정된다. Task 17의 매니페스트가 이 값을 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_kb_provenance.py` 끝에 추가:

```python
def test_issued_file_status_is_ranking_eligible(tmp_path):
    """공개 URL이 없는 보험사 공식 발행 파일도 순위 대상으로 인정한다.

    2026년 약관은 방화벽 안쪽 발행 경로에서 받아 공개 URL이 없다. 문서 자체가 보험사
    공식 발행본이므로 2차 유통본과 같게 취급하지 않는다.
    """
    manifest = tmp_path / "manifest.json"
    manifest.write_text(
        json.dumps(
            {
                "sources": [
                    {
                        "insurer": "SAMSUNG",
                        "verification_status": "VERIFIED_ISSUED_FILE",
                        "ranking_eligible": True,
                    },
                    {
                        "insurer": "MERITZ",
                        "verification_status": "SECONDARY_SOURCE",
                        "ranking_eligible": False,
                    },
                ]
            },
            ensure_ascii=False,
        ),
        encoding="utf-8",
    )
    assert ranking_eligible_insurer_codes(manifest) == {"SAMSUNG"}
```

파일 상단 import에 `json`과 `ranking_eligible_insurer_codes`가 이미 있는지 확인하고 없으면 추가한다.

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_kb_provenance.py::test_issued_file_status_is_ranking_eligible -v`
Expected: FAIL — `assert set() == {'SAMSUNG'}`

- [ ] **Step 3: 상태를 추가**

`backend/app/services/kb_provenance.py`:

```python
# VERIFIED_ISSUED_FILE: 공개 URL은 없지만 보험사가 공식 발행한 약관 파일.
# 2차 유통본(SECONDARY_SOURCE)과 달리 발행 주체가 보험사 자신이므로 순위 대상으로 둔다.
RANKING_ELIGIBLE_STATUSES = {"VERIFIED_CURRENT", "VERIFIED_VERSIONED", "VERIFIED_ISSUED_FILE"}
```

`backend/scripts/validate_kb.py`:

```python
VERIFICATION_STATUSES = {
    "VERIFIED_CURRENT",
    "VERIFIED_VERSIONED",
    "VERIFIED_ISSUED_FILE",
    "NEEDS_CURRENT_VERSION_CHECK",
    "SECONDARY_SOURCE",
    "EXTRACTION_INCOMPLETE",
}
RANKING_ELIGIBLE_STATUSES = {"VERIFIED_CURRENT", "VERIFIED_VERSIONED", "VERIFIED_ISSUED_FILE"}
```

- [ ] **Step 4: 통과를 확인**

Run: `cd backend && python -m pytest tests/test_kb_provenance.py -v`
Expected: 전부 PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/app/services/kb_provenance.py backend/scripts/validate_kb.py backend/tests/test_kb_provenance.py
git commit -m "공개 URL 없는 보험사 공식 발행본을 순위 대상으로 인정"
```

---

### Task 3: 조항 grounding 자동 대조 스크립트

모든 `Clause.text`가 해당 PDF 추출 텍스트의 부분 문자열인지 검사한다. 이번 드리프트를 사람이 알아채기까지 오래 걸렸다는 것이 이 스크립트가 필요한 이유다. Task 6 이후 모든 정독 결과를 이걸로 즉시 검증한다.

**Files:**
- Create: `backend/scripts/verify_clause_grounding.py`
- Test: `backend/tests/test_clause_grounding.py`

**Interfaces:**
- Consumes: Task 1의 `data/raw_pdfs/source_files.json`, `data/processed/*_full_text.txt`
- Produces: `verify_clause_grounding.check(db_path, catalog_path, processed_dir) -> list[dict]` — 어긋난 조항 목록. 빈 리스트면 100% 일치.
- Produces: `verify_clause_grounding.normalize(text) -> str` — 공백을 모두 제거한 대조용 문자열.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_clause_grounding.py`:

```python
"""조항 원문이 실제 PDF 추출본에 있는지 대조하는 검증 스크립트 테스트."""
import json
import sqlite3

from scripts.verify_clause_grounding import check, normalize


def _make_db(path, clause_text):
    connection = sqlite3.connect(path)
    connection.executescript(
        """
        CREATE TABLE insurer (insurer_id INTEGER PRIMARY KEY, code TEXT);
        CREATE TABLE product (product_id INTEGER PRIMARY KEY, insurer_id INTEGER, name TEXT);
        CREATE TABLE policy_version (policy_version_id INTEGER PRIMARY KEY, product_id INTEGER);
        CREATE TABLE clause (
            clause_id INTEGER PRIMARY KEY, policy_version_id INTEGER,
            article_no TEXT, text TEXT, page_ref TEXT
        );
        INSERT INTO insurer VALUES (1, 'SAMSUNG');
        INSERT INTO product VALUES (1, 1, '해외여행보험');
        INSERT INTO policy_version VALUES (1, 1);
        """
    )
    connection.execute(
        "INSERT INTO clause VALUES (1, 1, '제3조', ?, 'p.80')", (clause_text,)
    )
    connection.commit()
    connection.close()


def _make_sources(tmp_path):
    processed = tmp_path / "processed"
    processed.mkdir()
    (processed / "samsung_full_text.txt").write_text(
        "\n\n===PAGE 80===\n회사는 피보험자가 여행 도중에 상해를\n입은 경우 보험금을 지급합니다.\n",
        encoding="utf-8",
    )
    catalog = tmp_path / "source_files.json"
    catalog.write_text(
        json.dumps(
            {"SAMSUNG": [{"page_ref_prefix": None, "pdf": "samsung.pdf",
                          "text": "samsung_full_text.txt", "pages": 307, "sha256": "x"}]},
            ensure_ascii=False,
        ),
        encoding="utf-8",
    )
    return catalog, processed


def test_normalize_drops_all_whitespace():
    assert normalize("회사는  피보험자가\n여행 도중에") == "회사는피보험자가여행도중에"


def test_grounded_clause_passes(tmp_path):
    catalog, processed = _make_sources(tmp_path)
    db_path = tmp_path / "app.db"
    _make_db(db_path, "회사는 피보험자가 여행 도중에 상해를 입은 경우 보험금을 지급합니다.")
    assert check(db_path, catalog, processed) == []


def test_invented_clause_is_reported(tmp_path):
    catalog, processed = _make_sources(tmp_path)
    db_path = tmp_path / "app.db"
    _make_db(db_path, "회사는 피보험자가 여행 도중에 질병에 걸린 경우 보험금을 지급합니다.")
    failures = check(db_path, catalog, processed)
    assert len(failures) == 1
    assert failures[0]["insurer"] == "SAMSUNG"
    assert failures[0]["clause_id"] == 1
    assert failures[0]["article_no"] == "제3조"
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_clause_grounding.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.verify_clause_grounding'`

- [ ] **Step 3: 스크립트를 쓴다**

`backend/scripts/verify_clause_grounding.py`:

```python
"""모든 Clause.text가 원본 PDF 추출본에 문자 그대로 존재하는지 검사한다.

2026-08-18에 원본 약관이 교체되면서, 기존 조항 363개 중 새 판본에 원문 그대로 남은
것이 130개(36%)뿐이라는 사실을 뒤늦게 발견했다. 조항 원문이 근거의 뿌리이고
ClauseTerm.raw_text 같은 파생물이 전부 그 부분 문자열이어야 하므로, 원문 드리프트는
데이터 전체를 무효로 만든다. 이 검사를 상시로 돌려 다음 교체를 즉시 잡는다.

Run from ``backend``::

    python -m scripts.verify_clause_grounding
    python -m scripts.verify_clause_grounding --json
"""
from __future__ import annotations

import argparse
import json
import re
import sqlite3
import sys
from pathlib import Path

BACKEND_DIR = Path(__file__).resolve().parents[1]
DEFAULT_DATABASE = BACKEND_DIR / "data" / "app.db"
DEFAULT_CATALOG = BACKEND_DIR / "data" / "raw_pdfs" / "source_files.json"
DEFAULT_PROCESSED = BACKEND_DIR / "data" / "processed"

_PAGE_MARKER = re.compile(r"===PAGE \d+===")
_WHITESPACE = re.compile(r"\s+")


def normalize(text: str) -> str:
    """대조용 정규화: 페이지 마커를 지우고 모든 공백을 제거한다.

    pdfplumber는 같은 문장을 조판 폭에 따라 다른 위치에서 줄바꿈한다. 조항을 옮겨
    담을 때 줄바꿈이 공백 하나로 바뀌는 일이 흔하므로, 공백은 대조 대상에서 뺀다.
    글자 자체가 다르면 잡힌다.
    """
    return _WHITESPACE.sub("", _PAGE_MARKER.sub(" ", text or ""))


def load_haystacks(catalog_path: Path, processed_dir: Path) -> dict[str, str]:
    """보험사 코드 -> 그 보험사 원본 파일 전체를 이어붙인 정규화 텍스트."""
    catalog = json.loads(Path(catalog_path).read_text(encoding="utf-8"))
    haystacks: dict[str, str] = {}
    for insurer, entries in catalog.items():
        chunks = []
        for entry in entries:
            text_path = Path(processed_dir) / entry["text"]
            if not text_path.exists():
                raise FileNotFoundError(
                    f"extracted text is missing: {text_path}. Run scripts.extract_raw_pdf_text first."
                )
            chunks.append(text_path.read_text(encoding="utf-8"))
        haystacks[insurer] = normalize("\n".join(chunks))
    return haystacks


def check(
    db_path: Path = DEFAULT_DATABASE,
    catalog_path: Path = DEFAULT_CATALOG,
    processed_dir: Path = DEFAULT_PROCESSED,
) -> list[dict]:
    """원본에 없는 조항 목록을 돌려준다. 빈 리스트면 100% 일치."""
    haystacks = load_haystacks(catalog_path, processed_dir)
    connection = sqlite3.connect(db_path)
    connection.row_factory = sqlite3.Row
    try:
        rows = connection.execute(
            """SELECT i.code AS insurer, c.clause_id, c.article_no, c.page_ref, c.text
                 FROM clause c
                 JOIN policy_version pv USING(policy_version_id)
                 JOIN product p USING(product_id)
                 JOIN insurer i USING(insurer_id)
                ORDER BY i.code, c.clause_id"""
        ).fetchall()
    finally:
        connection.close()

    failures = []
    for row in rows:
        haystack = haystacks.get(row["insurer"])
        if haystack is None:
            failures.append(
                {
                    "insurer": row["insurer"],
                    "clause_id": row["clause_id"],
                    "article_no": row["article_no"],
                    "page_ref": row["page_ref"],
                    "reason": "원본 파일 목록에 이 보험사가 없다",
                }
            )
            continue
        if normalize(row["text"]) not in haystack:
            failures.append(
                {
                    "insurer": row["insurer"],
                    "clause_id": row["clause_id"],
                    "article_no": row["article_no"],
                    "page_ref": row["page_ref"],
                    "reason": "원본 추출본에 이 문구가 없다",
                    "head": (row["text"] or "")[:80],
                }
            )
    return failures


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--database", type=Path, default=DEFAULT_DATABASE)
    parser.add_argument("--catalog", type=Path, default=DEFAULT_CATALOG)
    parser.add_argument("--processed", type=Path, default=DEFAULT_PROCESSED)
    parser.add_argument("--json", action="store_true", dest="as_json")
    args = parser.parse_args(argv)

    failures = check(args.database, args.catalog, args.processed)
    if args.as_json:
        print(json.dumps(failures, ensure_ascii=False, indent=2))
    elif failures:
        print(f"grounding FAIL: {len(failures)}건이 원본과 어긋난다")
        for failure in failures:
            print(f"  [{failure['insurer']}] clause_id={failure['clause_id']} "
                  f"{failure['article_no']} ({failure.get('page_ref')}) - {failure['reason']}")
            if failure.get("head"):
                print(f"      {failure['head']}")
    else:
        print("grounding OK: 모든 조항이 원본 추출본에 존재한다")
    return 1 if failures else 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과를 확인**

Run: `cd backend && python -m pytest tests/test_clause_grounding.py -v`
Expected: 4개 PASS

- [ ] **Step 5: 현재 DB에 돌려 드리프트를 재확인**

Run: `cd backend && python -m scripts.verify_clause_grounding`
Expected: `grounding FAIL: 233건` 내외(교체 전 구 조항이라 대부분 어긋나는 것이 정상). Task 4에서 이 조항들을 지운다.

- [ ] **Step 6: 커밋**

```bash
git add backend/scripts/verify_clause_grounding.py backend/tests/test_clause_grounding.py
git commit -m "조항 원문이 원본 PDF에 실제로 있는지 대조하는 검증 스크립트 추가"
```

---

### Task 4: 판 갈기 (reset_kb.py)

구 KB와 개발용 사용자 데이터를 지운다. 계정까지 지우므로 실행 전 대상 행 수를 출력하고 확인을 받는다.

**Files:**
- Create: `backend/app/reset_kb.py`
- Test: `backend/tests/test_reset_kb.py`

**Interfaces:**
- Produces: `reset_kb.DELETE_ORDER: tuple[str, ...]` — 외래키 역순 삭제 순서.
- Produces: `reset_kb.counts(db) -> dict[str, int]`, `reset_kb.run(confirm: bool = False) -> dict[str, int]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_reset_kb.py`:

```python
"""구 KB 삭제 범위 테스트.

이 스크립트는 되돌릴 수 없으므로, 지우는 테이블과 남기는 테이블을 테스트로 고정한다.
"""
from app.reset_kb import DELETE_ORDER, KEEP_TABLES


def test_delete_order_covers_kb_and_user_data():
    for table in (
        "clause_incident_map", "clause_term", "coverage_doc_map", "doc_requirement",
        "overlap_rule", "clause_standard_map", "clause", "coverage",
        "policy_version", "product",
        "app_user", "user_policy", "user_coverage", "trip", "incident", "evidence",
        "analysis_run", "analysis_finding", "finding_evidence_link",
        "validation_result", "user_question_log", "eval_log",
    ):
        assert table in DELETE_ORDER, f"{table}이 삭제 목록에 없다"


def test_reference_data_is_kept():
    for table in (
        "insurer", "coverage_std", "incident_type", "required_doc_std", "standard_clause",
        "insurer_premium", "travel_alert", "nonpayment_rate", "flight_delay_stat",
        "country_language", "onsite_phrase_i18n", "question_bank",
        "simulation_scenario", "validation_rule",
    ):
        assert table in KEEP_TABLES, f"{table}은 남겨야 한다"
        assert table not in DELETE_ORDER, f"{table}이 삭제 목록에 들어 있다"


def test_children_are_deleted_before_parents():
    """외래키 역순이 아니면 SQLite가 참조 오류를 낸다."""
    assert DELETE_ORDER.index("finding_evidence_link") < DELETE_ORDER.index("analysis_finding")
    assert DELETE_ORDER.index("analysis_finding") < DELETE_ORDER.index("analysis_run")
    assert DELETE_ORDER.index("clause_incident_map") < DELETE_ORDER.index("clause")
    assert DELETE_ORDER.index("clause") < DELETE_ORDER.index("coverage")
    assert DELETE_ORDER.index("coverage") < DELETE_ORDER.index("policy_version")
    assert DELETE_ORDER.index("user_coverage") < DELETE_ORDER.index("user_policy")
    assert DELETE_ORDER.index("user_policy") < DELETE_ORDER.index("app_user")
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_reset_kb.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.reset_kb'`

- [ ] **Step 3: 스크립트를 쓴다**

`backend/app/reset_kb.py`:

```python
"""2026년 약관으로 갈아엎기 전에 구 KB와 개발용 사용자 데이터를 지운다.

되돌릴 수 없다. 구 원본 PDF는 저장소에 없고(.gitignore) 디스크에도 남아 있지 않아
구 조항을 다시 만들 방법이 없다. 그래서 --confirm 없이는 세는 것만 하고 끝낸다.

남기는 것은 약관과 무관한 사전·외부 자료다(coverage_std, incident_type,
required_doc_std, standard_clause, 보험료·여행경보·부지급률·지연통계 등).
insurer 6행도 남긴다 - 회사 자체는 바뀌지 않았다.

Run from ``backend``::

    python -m app.reset_kb            # 삭제 대상 행 수만 출력
    python -m app.reset_kb --confirm  # 실제 삭제
"""
from __future__ import annotations

import argparse
import sys

from sqlalchemy import text

from app.database import SessionLocal

# 외래키 역순: 자식 -> 부모. 순서를 바꾸면 SQLite가 참조 오류를 낸다.
DELETE_ORDER: tuple[str, ...] = (
    # 분석 결과와 근거 링크
    "finding_evidence_link",
    "analysis_finding",
    "validation_result",
    "user_question_log",
    "eval_log",
    "evidence",
    "analysis_run",
    # 사용자 여행·사고·보유 계약
    "incident",
    "trip",
    "user_coverage",
    "user_policy",
    "external_coverage",
    "external_policy",
    "app_user",
    # 조항 파생
    "clause_standard_map",
    "overlap_rule",
    "doc_requirement",
    "coverage_doc_map",
    "clause_term",
    "clause_incident_map",
    # 조항과 담보, 상품
    "clause",
    "coverage",
    "policy_version",
    "product",
)

KEEP_TABLES: frozenset[str] = frozenset(
    {
        "insurer",
        "coverage_std",
        "incident_type",
        "required_doc_std",
        "standard_clause",
        "insurer_premium",
        "travel_alert",
        "nonpayment_rate",
        "flight_delay_stat",
        "country_language",
        "onsite_phrase_i18n",
        "question_bank",
        "simulation_scenario",
        "validation_rule",
    }
)


def counts(db) -> dict[str, int]:
    result = {}
    for table in DELETE_ORDER:
        result[table] = db.execute(text(f"SELECT COUNT(*) FROM {table}")).scalar() or 0
    return result


def run(confirm: bool = False) -> dict[str, int]:
    db = SessionLocal()
    try:
        before = counts(db)
        total = sum(before.values())
        for table, rows in before.items():
            if rows:
                print(f"  {table}: {rows}")
        print(f"삭제 대상 합계: {total}행")
        if not confirm:
            print("--confirm 없이 실행했으므로 아무것도 지우지 않았다.")
            return before
        for table in DELETE_ORDER:
            db.execute(text(f"DELETE FROM {table}"))
        db.commit()
        after = counts(db)
        remaining = sum(after.values())
        print(f"삭제 완료. 남은 행: {remaining}")
        if remaining:
            raise RuntimeError(f"삭제되지 않은 행이 있다: { {k: v for k, v in after.items() if v} }")
        return before
    finally:
        db.close()


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--confirm", action="store_true", help="실제로 삭제한다")
    args = parser.parse_args(argv)
    run(confirm=args.confirm)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과를 확인**

Run: `cd backend && python -m pytest tests/test_reset_kb.py -v`
Expected: 3개 PASS

- [ ] **Step 5: 삭제 대상을 먼저 눈으로 확인**

Run: `cd backend && python -m app.reset_kb`
Expected: 표에 `app_user: 730`, `clause: 363`, `analysis_finding: 1388` 등이 나오고 아무것도 지워지지 않는다.

- [ ] **Step 6: DB 백업 후 실제 삭제**

```bash
cd backend && cp data/app.db data/app.db.bak-before-2026-rebuild
python -m app.reset_kb --confirm
python -m scripts.verify_clause_grounding
```
Expected: 삭제 완료, 남은 행 0. grounding은 `grounding OK`(조항이 없으므로 어긋나는 것도 없다).

백업 파일명은 `.gitignore`의 `backend/data/*.db.bak*` 패턴에 걸려 커밋되지 않는다.

- [ ] **Step 7: 커밋**

```bash
git add backend/app/reset_kb.py backend/tests/test_reset_kb.py
git commit -m "구 KB와 개발용 사용자 데이터 삭제 스크립트 추가"
```

---

### Task 5: 청크 경계 실측

보험사별로 특별약관이 시작하는 실제 페이지를 뽑아 정독 청크 경계를 확정한다. 스펙의 경계는 헤딩 스캔 초안이고, DB손보는 목차가 3~5쪽에 뭉쳐 있어 본문에서 다시 잡아야 한다.

**Files:**
- Create: `backend/scripts/map_terms_structure.py`
- Create: `docs/superpowers/plans/2026-08-18-chunk-boundaries.md`

**Interfaces:**
- Produces: `map_terms_structure.headings(text: str) -> list[tuple[int, str]]` — (페이지, 약관 제목) 목록. 본문 인용 중 걸린 오탐(문장 중간에서 끊긴 제목)은 제외한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_terms_structure.py`:

```python
"""약관 제목 헤딩 추출 테스트."""
from scripts.map_terms_structure import headings

SAMPLE = """

===PAGE 43===
배상책임 특별약관
제1조(보상하는 손해)
회사는 피보험자가 여행도중에 생긴 보험사고로 인하여 피해자에게 법률상의

===PAGE 44===
가적으로 부담한 비용 손해를 이 특별약관
에 따라 보상합니다.

===PAGE 58===
해외여행중 중대사고 구조송환비용 특별약관
"""


def test_headings_are_page_and_title_pairs():
    assert (43, "배상책임 특별약관") in headings(SAMPLE)
    assert (58, "해외여행중 중대사고 구조송환비용 특별약관") in headings(SAMPLE)


def test_mid_sentence_reference_is_not_a_heading():
    """'... 이 특별약관' 처럼 문장이 끊긴 줄은 제목이 아니다."""
    titles = [title for _, title in headings(SAMPLE)]
    assert not any(title.startswith("가적으로") for title in titles)
    assert all("이 특별약관" != title for title in titles)
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_terms_structure.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 스크립트를 쓴다**

`backend/scripts/map_terms_structure.py`:

```python
"""약관 전체 텍스트에서 보통약관·특별약관 제목과 시작 페이지를 뽑는다.

정독 청크를 나눌 때 쓴다. 제목처럼 보이지만 본문 인용인 줄(조사로 시작하거나 문장이
중간에서 끊긴 줄)은 걸러낸다 - 오탐을 청크 경계로 쓰면 담당 범위가 어긋난다.

Run from ``backend``::

    python -m scripts.map_terms_structure
    python -m scripts.map_terms_structure --insurer DB
"""
from __future__ import annotations

import argparse
import json
import re
import sys
from pathlib import Path

BACKEND_DIR = Path(__file__).resolve().parents[1]
DEFAULT_CATALOG = BACKEND_DIR / "data" / "raw_pdfs" / "source_files.json"
DEFAULT_PROCESSED = BACKEND_DIR / "data" / "processed"

# 줄 전체가 제목인 경우만 잡는다.
_TITLE = re.compile(
    r"^\s*(?:\[\s*)?([가-힣][가-힣A-Za-z0-9·()\[\]/,\.\-Ⅰ-Ⅹ ]{3,44}"
    r"(?:추가특별약관|특별약관|보통약관))(?:\s*\])?\s*$",
    re.M,
)
# 제목 앞에 붙으면 본문 인용인 신호(조사·접속사로 시작하거나 지시어가 들어간 경우).
_REFERENCE_HINT = re.compile(r"^(?:이|그|해당|동|위|같은|또는|및|을|를|은|는|에|의)\s")


def headings(text: str) -> list[tuple[int, str]]:
    found: list[tuple[int, str]] = []
    for chunk in text.split("===PAGE ")[1:]:
        number, _, body = chunk.partition("===\n")
        try:
            page = int(number.strip())
        except ValueError:
            continue
        for match in _TITLE.finditer(body):
            title = match.group(1).strip()
            if _REFERENCE_HINT.match(title):
                continue
            if title.endswith(("이 특별약관", "이 추가특별약관", "해당 특별약관")):
                continue
            found.append((page, title))
    return found


def run(insurer: str | None = None, catalog_path: Path = DEFAULT_CATALOG,
        processed_dir: Path = DEFAULT_PROCESSED) -> dict[str, list[dict]]:
    catalog = json.loads(catalog_path.read_text(encoding="utf-8"))
    report: dict[str, list[dict]] = {}
    for code, entries in catalog.items():
        if insurer and code != insurer:
            continue
        rows = []
        for entry in entries:
            text = (processed_dir / entry["text"]).read_text(encoding="utf-8")
            first_seen: dict[str, int] = {}
            for page, title in headings(text):
                first_seen.setdefault(title, page)
            rows.append({"pdf": entry["pdf"], "prefix": entry["page_ref_prefix"],
                         "pages": entry["pages"],
                         "headings": [{"page": page, "title": title}
                                      for title, page in sorted(first_seen.items(), key=lambda kv: kv[1])]})
        report[code] = rows
        for row in rows:
            print(f"=== {code} {row['pdf']} ({row['pages']} pages)")
            for heading in row["headings"]:
                print(f"   p.{heading['page']:>4}  {heading['title']}")
    return report


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--insurer")
    parser.add_argument("--catalog", type=Path, default=DEFAULT_CATALOG)
    parser.add_argument("--processed", type=Path, default=DEFAULT_PROCESSED)
    args = parser.parse_args(argv)
    run(args.insurer, args.catalog, args.processed)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과를 확인**

Run: `cd backend && python -m pytest tests/test_terms_structure.py -v`
Expected: 2개 PASS

- [ ] **Step 5: 6개사 구조를 뽑아 청크 경계를 확정**

Run: `cd backend && python -m scripts.map_terms_structure > ../docs/superpowers/plans/2026-08-18-chunk-boundaries.md`

출력을 읽고, 보험사별로 다음 형식의 표를 같은 파일 맨 위에 추가한다. 청크는 보통약관 / 실손의료비 / 나머지 보장 특약 / 계약형태 특약 / 별표·인용법규로 나누고, 한 청크가 100쪽을 넘지 않게 쪼갠다.

```markdown
## 확정 청크

| 청크 ID | 보험사 | 페이지 | 담당 범위 |
|---|---|---|---|
| samsung-A | SAMSUNG | 1-55 | 안내사항·주요내용 요약서·보통약관 |
```

- [ ] **Step 6: 커밋**

```bash
git add backend/scripts/map_terms_structure.py backend/tests/test_terms_structure.py docs/superpowers/plans/2026-08-18-chunk-boundaries.md
git commit -m "약관 구조 스캔 스크립트와 확정 청크 경계 추가"
```

---

### Task 6: 삼성화재 정독과 시드 (307쪽)

Task 5에서 확정한 청크를 순서대로 정독한다. 청크마다 시드 파일 하나를 만들고, 넣은 즉시 검증한다.

**Files:**
- Create: `backend/app/seed_samsung_2026_a.py` … `_f.py` (Task 5의 확정 청크 수만큼)
- Modify: `backend/data/app.db` (시드 실행)

**Interfaces:**
- Consumes: `data/processed/samsung_overseas_2026_full_text.txt`, `app.services.kb_seed_common.get_or_create_coverage_std`, `raw_text_is_grounded`
- Produces: `Product(name='해외여행보험', product_code='SAMSUNG-OVERSEAS-2026')` 1행, `PolicyVersion(version_label='2026수집본')` 1행, 그 아래 `Coverage`·`Clause`.

- [ ] **Step 1: 상품과 정책 버전을 만드는 첫 청크 시드를 쓴다**

`backend/app/seed_samsung_2026_a.py` — 아래 골격을 쓰고, 담당 페이지 범위를 실제로 읽어 `CLAUSES` 목록을 채운다. `text`는 추출본에서 복사한 원문 그대로다.

```python
"""삼성화재 2026년 약관 청크 A - 안내사항과 보통약관.

원문: data/raw_pdfs/samsung_overseas_2026.pdf (307쪽, 본문 머리글 "n / 296")
추출본: data/processed/samsung_overseas_2026_full_text.txt
담당: p.1-55

발견한 내용과 매핑은 아래 CLAUSES의 각 항목 주석에 적는다.
건너뛴 부분: (없으면 "없음", 있으면 페이지와 이유)
확인함, 무관: (읽었지만 사고유형과 무관해 넣지 않은 조항 목록)
"""
from __future__ import annotations

from app.database import SessionLocal
from app.models.kb import Clause, Coverage, Insurer, PolicyVersion, Product
from app.services.kb_seed_common import get_or_create_coverage_std

PRODUCT_CODE = "SAMSUNG-OVERSEAS-2026"
VERSION_LABEL = "2026수집본"

# (coverage_std_code, raw_name, definition, limit_amount, deductible, waiting_condition)
COVERAGES: list[tuple[str, str, str | None, str | None, str | None, str | None]] = [
]

# (coverage_std_code | None, clause_type, article_no, text, page_ref, default_color)
CLAUSES: list[tuple[str | None, str, str, str, str, str]] = [
]


def _product(db) -> Product:
    insurer = db.query(Insurer).filter_by(code="SAMSUNG").one()
    product = db.query(Product).filter_by(product_code=PRODUCT_CODE).first()
    if product is None:
        product = Product(
            insurer_id=insurer.insurer_id,
            name="해외여행보험",
            product_code=PRODUCT_CODE,
            channel="다이렉트",
            collected_at=None,
            review_status="raw",
        )
        db.add(product)
        db.flush()
    return product


def _policy_version(db, product: Product) -> PolicyVersion:
    version = db.query(PolicyVersion).filter_by(
        product_id=product.product_id, version_label=VERSION_LABEL
    ).first()
    if version is None:
        version = PolicyVersion(
            product_id=product.product_id,
            version_label=VERSION_LABEL,
            effective_date=None,  # 표지가 이미지라 시행일이 인쇄돼 있지 않다
            approval_no=None,
            source_url=None,      # 보험사 공식 발행 파일, 공개 URL 없음
            file_hash="95fc1ac011933d8b329eeee3698cfd8adffa0540bf353b0e90dca446156406b7",
        )
        db.add(version)
        db.flush()
    return version


def run() -> None:
    db = SessionLocal()
    try:
        product = _product(db)
        version = _policy_version(db, product)

        by_code = {}
        for std_code, raw_name, definition, limit_amount, deductible, waiting in COVERAGES:
            std = get_or_create_coverage_std(db, std_code, raw_name, "상해", False) if std_code else None
            existing = db.query(Coverage).filter_by(
                policy_version_id=version.policy_version_id, raw_name=raw_name
            ).first()
            if existing is None:
                existing = Coverage(
                    policy_version_id=version.policy_version_id,
                    coverage_std_id=std.coverage_std_id if std else None,
                    raw_name=raw_name,
                    definition=definition,
                    limit_amount=limit_amount,
                    deductible=deductible,
                    waiting_condition=waiting,
                )
                db.add(existing)
                db.flush()
            by_code[std_code or raw_name] = existing

        added = 0
        for std_code, clause_type, article_no, text, page_ref, color in CLAUSES:
            coverage = by_code.get(std_code)
            duplicate = db.query(Clause).filter_by(
                policy_version_id=version.policy_version_id, text=text
            ).first()
            if duplicate is not None:
                continue
            db.add(
                Clause(
                    policy_version_id=version.policy_version_id,
                    coverage_id=coverage.coverage_id if coverage else None,
                    clause_type=clause_type,
                    article_no=article_no,
                    text=text,
                    page_ref=page_ref,
                    default_color=color,
                )
            )
            added += 1
        db.commit()
        print(f"[samsung 2026 A] coverages={len(by_code)} clauses_added={added}")
    finally:
        db.close()


if __name__ == "__main__":
    run()
```

- [ ] **Step 2: 담당 범위를 실제로 읽는다**

Run:
```bash
cd backend && python -c "
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
text = open('data/processed/samsung_overseas_2026_full_text.txt', encoding='utf-8').read()
pages = text.split('===PAGE ')
for chunk in pages[1:]:
    number, _, body = chunk.partition('===\n')
    if 1 <= int(number) <= 55:
        print('===PAGE', number, '==='); print(body)
"
```

읽은 내용으로 `COVERAGES`와 `CLAUSES`를 채운다. `text`는 추출본에서 그대로 복사한다.

- [ ] **Step 3: 시드를 실행**

Run: `cd backend && python -m app.seed_samsung_2026_a`
Expected: `[samsung 2026 A] coverages=N clauses_added=M`

- [ ] **Step 4: grounding 검증**

Run: `cd backend && python -m scripts.verify_clause_grounding`
Expected: `grounding OK`. FAIL이 나오면 그 조항의 `text`를 추출본에서 다시 복사한다(원문을 고치지 말고 복사를 고친다).

- [ ] **Step 5: 남은 청크를 같은 방식으로 반복**

청크 B~F 각각에 대해 Step 1~4를 반복한다. B 이후 시드는 `_product`/`_policy_version`을 다시 쓰지 않고 다음 조회로 기존 행을 찾는다.

```python
def _existing_version(db) -> PolicyVersion:
    product = db.query(Product).filter_by(product_code=PRODUCT_CODE).one()
    return db.query(PolicyVersion).filter_by(
        product_id=product.product_id, version_label=VERSION_LABEL
    ).one()
```

- [ ] **Step 6: 커밋**

```bash
git add backend/app/seed_samsung_2026_*.py
git commit -m "삼성화재 2026년 약관 307쪽 정독 시드"
```

---

### Task 7: 현대해상 정독과 시드 (162쪽)

**Files:**
- Create: `backend/app/seed_hyundai_2026_a.py` … (Task 5의 확정 청크 수만큼)

**Interfaces:**
- Consumes: `data/processed/hyundai_overseas_8403-0000-20260606_full_text.txt`
- Produces: `Product(product_code='HYUNDAI-OVERSEAS-2026')`, `PolicyVersion(version_label='8403-0000-20260606', effective_date=date(2026, 6, 6), file_hash='19fcd2966b4d17fdca41bd8edfdfff028a4de3ba38ed3574e83406262ec3c878')`

- [ ] **Step 1~6:** Task 6과 같은 절차. 상품명은 `다이렉트 해외여행보험`, `channel='다이렉트'`. 시행일은 약관분류코드 뒷자리에서 확인된 `2026-06-06`을 넣는다.

```python
from datetime import date
...
            effective_date=date(2026, 6, 6),
            approval_no="8403-0000-20260606",
            source_url=None,
            file_hash="19fcd2966b4d17fdca41bd8edfdfff028a4de3ba38ed3574e83406262ec3c878",
```

- [ ] **Step 7: 커밋**

```bash
git add backend/app/seed_hyundai_2026_*.py
git commit -m "현대해상 2026년 약관 162쪽 정독 시드"
```

---

### Task 8: 메리츠화재 정독과 시드 (244쪽)

**Files:**
- Create: `backend/app/seed_meritz_2026_a.py` …

**Interfaces:**
- Consumes: `data/processed/meritz_overseas_2607A_full_text.txt`
- Produces: `Product(product_code='MERITZ-OVERSEAS-2026', name='다이렉트 해외여행보험')`, `PolicyVersion(version_label='메리츠일반-특종/상해/여행B-10-2607A', effective_date=None, file_hash='f58406b6496f1031da5f5c870e73c65b7b85ef06d16e2b04448b53d21b61dbdd')`

`effective_date`는 `None`이다. 상품코드 뒷자리 `2607`을 2026년 7월로 읽을 수는 있지만 시행일이라는 근거가 없다. 추정하지 않는다.

- [ ] **Step 1~6:** Task 6과 같은 절차.
- [ ] **Step 7: 커밋**

```bash
git add backend/app/seed_meritz_2026_*.py
git commit -m "메리츠화재 2026년 약관 244쪽 정독 시드"
```

---

### Task 9: KB손해보험 정독과 시드 (326쪽)

**Files:**
- Create: `backend/app/seed_kb_2026_a.py` …

**Interfaces:**
- Consumes: `data/processed/kb_overseas_26-15505-1_full_text.txt`
- Produces: `Product(product_code='KB-OVERSEAS-2026', name='KB해외여행보험(다이렉트)')`, `PolicyVersion(version_label='일반26-15505-1', effective_date=None, approval_no='일반26-15505-1', file_hash='49a5ccecdb5cffe5bcf730efedf13322e0719cff8261aebcf47b724cc064e573')`

KB에만 있는 담보가 여럿이다(기후성질환 온열·한랭 진단비, 식중독입원일당, 지수형 항공기 지연, 부부·가족가입). 새 `CoverageStd`가 필요하면 이 태스크에서 만든다.

- [ ] **Step 1~6:** Task 6과 같은 절차.
- [ ] **Step 7: 커밋**

```bash
git add backend/app/seed_kb_2026_*.py
git commit -m "KB손해보험 2026년 약관 326쪽 정독 시드"
```

---

### Task 10: DB손해보험 정독과 시드 (223쪽)

**Files:**
- Create: `backend/app/seed_db_2026_a.py` …

**Interfaces:**
- Consumes: `data/processed/db_overseas_promi1_2026_full_text.txt`
- Produces: `Product(product_code='DB-OVERSEAS-2026', name='프로미 해외여행보험Ⅰ')`, `PolicyVersion(version_label='프로미Ⅰ_2026수집본', effective_date=None, file_hash='151c57ec603cad5b5d5dcc4128468e2ac9102d45d842554f1d98fbbecbf3e008')`

목차가 p.1-5에 뭉쳐 있어 특별약관 시작 페이지를 본문에서 찾아야 한다. Task 5에서 확정한 경계를 쓴다.

- [ ] **Step 1~6:** Task 6과 같은 절차.
- [ ] **Step 7: 커밋**

```bash
git add backend/app/seed_db_2026_*.py
git commit -m "DB손해보험 2026년 약관 223쪽 정독 시드"
```

---

### Task 11: 카카오페이 정독과 시드 (3파일 979쪽, 1개 상품으로 통합)

세 파일을 모두 전 페이지 읽는다. 카카오3(`해외여행보험`)을 기준으로 조항을 만들고, 카카오1·2에만 있는 조항을 추가한다. 본문이 같은 조항은 중복 행을 만들지 않는다.

**Files:**
- Create: `backend/app/seed_kakaopay_2026_a.py` … (카카오3 기준 청크)
- Create: `backend/app/seed_kakaopay_2026_together_diff.py` (카카오1·2 차이분)

**Interfaces:**
- Consumes: `data/processed/kakaopay_overseas_2026-0199_standard_full_text.txt`, `..._together1_full_text.txt`, `..._together2_full_text.txt`
- Produces: `Product(product_code='KAKAOPAY-OVERSEAS-2026', name='해외여행보험')`, `PolicyVersion(version_label='제2026-0199호', effective_date=date(2026, 5, 4), approval_no='제2026-0199호', file_hash='3ed801afa9c8e930b01aed97f4ba8d67823d9ee7d5e4d2d533a3a80f67b4009c')`

`file_hash`는 기준 파일(카카오3)의 해시를 쓴다. 나머지 두 파일의 해시는 Task 17의 매니페스트 `known_gap`에 함께 적는다.

- [ ] **Step 1: 카카오3 청크를 Task 6 절차로 정독·시드**

`page_ref`에 `K3 p.201` 형식으로 접두사를 붙인다.

- [ ] **Step 2: 카카오1을 전 페이지 읽는다**

Run:
```bash
cd backend && python -c "
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
text = open('data/processed/kakaopay_overseas_2026-0199_together1_full_text.txt', encoding='utf-8').read()
print(text)
" > ../../together1_read.txt
```

(임시 출력은 스크래치패드에 두고 커밋하지 않는다.)

- [ ] **Step 3: 카카오3에 없는 조항만 골라낸다**

Run:
```bash
cd backend && python -c "
import io, re, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
def norm(s): return re.sub(r'\s+', '', re.sub(r'===PAGE \d+===', ' ', s))
base = norm(open('data/processed/kakaopay_overseas_2026-0199_standard_full_text.txt', encoding='utf-8').read())
for name in ('together1', 'together2'):
    path = f'data/processed/kakaopay_overseas_2026-0199_{name}_full_text.txt'
    text = open(path, encoding='utf-8').read()
    for chunk in text.split('===PAGE ')[1:]:
        number, _, body = chunk.partition('===\n')
        for line in body.splitlines():
            stripped = line.strip()
            if len(stripped) >= 40 and norm(stripped) not in base:
                print(f'[{name} p.{number}] {stripped}')
" | head -200
```

- [ ] **Step 4: 차이분을 `seed_kakaopay_2026_together_diff.py`로 시드**

`page_ref`는 `K1 p.198` / `K2 p.198`. 상품명 표기 차이(`함께하는 해외여행보험` vs `II`)처럼 보장 내용이 아닌 차이는 넣지 않고 docstring에 "확인함, 표기 차이"로 남긴다.

- [ ] **Step 5: grounding 검증**

Run: `cd backend && python -m scripts.verify_clause_grounding`
Expected: `grounding OK`. 세 파일을 이어붙인 텍스트로 대조하므로 어느 파일에서 온 조항이든 통과한다.

- [ ] **Step 6: 커밋**

```bash
git add backend/app/seed_kakaopay_2026_*.py
git commit -m "카카오페이 2026년 약관 3파일 979쪽 정독 시드"
```

---

### Task 12: 사고유형 매핑 재생성

**Files:**
- Create: `backend/app/seed_2026_incident_map.py`
- Test: `backend/tests/test_incident_map_seed.py`

**Interfaces:**
- Consumes: Task 6~11이 만든 `Clause` 행
- Produces: `ClauseIncidentMap` 행. `relevance`는 `직접/조건부/면책`만 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_incident_map_seed.py`:

```python
"""사고유형 매핑 시드의 불변 조건."""
import sqlite3
from pathlib import Path

DB = Path(__file__).resolve().parents[1] / "data" / "app.db"


def _query(sql):
    connection = sqlite3.connect(DB)
    try:
        return connection.execute(sql).fetchall()
    finally:
        connection.close()


def test_no_legacy_relevance_value():
    """'제한'은 레거시 값이다. 새 매핑에는 없어야 한다."""
    assert _query("SELECT COUNT(*) FROM clause_incident_map WHERE relevance = '제한'")[0][0] == 0


def test_document_clauses_are_not_mapped():
    """clause_type='서류' 조항은 CoverageDocMap 경로로만 소비된다."""
    rows = _query(
        """SELECT COUNT(*) FROM clause_incident_map m
             JOIN clause c USING(clause_id) WHERE c.clause_type = '서류'"""
    )
    assert rows[0][0] == 0


def test_every_insurer_has_mappings():
    rows = _query(
        """SELECT i.code, COUNT(m.map_id) FROM insurer i
             JOIN product p USING(insurer_id)
             JOIN policy_version pv USING(product_id)
             JOIN clause c USING(policy_version_id)
             LEFT JOIN clause_incident_map m USING(clause_id)
            GROUP BY i.code"""
    )
    assert len(rows) == 6
    for code, count in rows:
        assert count > 0, f"{code}에 사고유형 매핑이 없다"
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_incident_map_seed.py -v`
Expected: `test_every_insurer_has_mappings` FAIL(매핑이 아직 없다)

- [ ] **Step 3: 시드를 쓴다**

보험사별 조항을 조회해 `incident_type` L2에 매핑한다. `seed_incident_types.py`의 TAXONOMY를 먼저 읽고, 어디에도 안 맞으면 `needs_review=True`인 새 L2를 만든다.

```python
"""2026년 약관 조항을 사고유형에 매핑한다.

조항 텍스트가 아니라 담보(coverage_std)와 clause_type을 기준으로 매핑한다 - 같은
담보면 보험사가 달라도 같은 사고유형에 걸린다는 것이 이 프로젝트의 전제다.
"""
from __future__ import annotations

from app.database import SessionLocal
from app.models.kb import Clause, ClauseIncidentMap, Coverage, CoverageStd, IncidentType

# coverage_std.std_code -> [(l2_code, relevance)]
MAPPING: dict[str, list[tuple[str, str]]] = {
}


def run() -> None:
    db = SessionLocal()
    try:
        types = {t.l2_code: t for t in db.query(IncidentType).all()}
        added = 0
        for std_code, targets in MAPPING.items():
            std = db.query(CoverageStd).filter_by(std_code=std_code).first()
            if std is None:
                continue
            clauses = (
                db.query(Clause)
                .join(Coverage, Clause.coverage_id == Coverage.coverage_id)
                .filter(Coverage.coverage_std_id == std.coverage_std_id)
                .filter(Clause.clause_type != "서류")
                .all()
            )
            for clause in clauses:
                for l2_code, relevance in targets:
                    incident_type = types.get(l2_code)
                    if incident_type is None:
                        raise KeyError(f"unknown incident type: {l2_code}")
                    exists = db.query(ClauseIncidentMap).filter_by(
                        clause_id=clause.clause_id, type_id=incident_type.type_id,
                        relevance=relevance,
                    ).first()
                    if exists:
                        continue
                    db.add(ClauseIncidentMap(
                        clause_id=clause.clause_id, type_id=incident_type.type_id,
                        relevance=relevance, mapped_by="human", confidence=None,
                    ))
                    added += 1
        db.commit()
        print(f"[2026 incident map] added={added}")
    finally:
        db.close()


if __name__ == "__main__":
    run()
```

- [ ] **Step 4: 실행하고 통과를 확인**

Run: `cd backend && python -m app.seed_2026_incident_map && python -m pytest tests/test_incident_map_seed.py -v`
Expected: 3개 PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/app/seed_2026_incident_map.py backend/tests/test_incident_map_seed.py
git commit -m "2026년 조항 사고유형 매핑 재생성"
```

---

### Task 13: 정량 조건(ClauseTerm) 재생성

**Files:**
- Create: `backend/app/seed_2026_clause_terms.py`
- Test: `backend/tests/test_clause_terms_seed.py`

**Interfaces:**
- Consumes: `app.services.kb_seed_common.raw_text_is_grounded`
- Produces: `ClauseTerm` 행. `term_type`은 `지급한도/자기부담금/면책일수/지연기준시간` 등.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_clause_terms_seed.py`:

```python
"""ClauseTerm.raw_text는 반드시 조항 원문의 부분 문자열이다."""
import sqlite3
from pathlib import Path

DB = Path(__file__).resolve().parents[1] / "data" / "app.db"


def test_every_raw_text_is_a_substring_of_its_clause():
    connection = sqlite3.connect(DB)
    try:
        rows = connection.execute(
            "SELECT t.term_id, t.raw_text, c.text FROM clause_term t JOIN clause c USING(clause_id)"
        ).fetchall()
    finally:
        connection.close()
    assert rows, "clause_term이 비어 있다"
    broken = [term_id for term_id, raw_text, text in rows if raw_text.strip() not in text]
    assert broken == [], f"원문에 없는 발췌: {broken}"


def test_delay_threshold_terms_have_hours():
    connection = sqlite3.connect(DB)
    try:
        rows = connection.execute(
            "SELECT term_id, unit FROM clause_term WHERE term_type = '지연기준시간'"
        ).fetchall()
    finally:
        connection.close()
    for term_id, unit in rows:
        assert unit == "시간", f"term_id={term_id}의 단위가 시간이 아니다"
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_clause_terms_seed.py -v`
Expected: `assert rows` FAIL — clause_term이 비어 있다

- [ ] **Step 3: 시드를 쓴다**

조항별로 금액·일수·시간 표현을 찾아 `ClauseTerm`을 만든다. `db.add` 전에 반드시 검증한다.

```python
        if not raw_text_is_grounded(clause.text, raw_text):
            raise ValueError(f"clause_id={clause.clause_id}: raw_text가 원문에 없다: {raw_text[:40]}")
```

- [ ] **Step 4: 실행하고 통과를 확인**

Run: `cd backend && python -m app.seed_2026_clause_terms && python -m pytest tests/test_clause_terms_seed.py -v`
Expected: 2개 PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/app/seed_2026_clause_terms.py backend/tests/test_clause_terms_seed.py
git commit -m "2026년 조항 정량 조건 재생성"
```

---

### Task 14: 청구서류 매핑과 서류 요건 재생성

**Files:**
- Create: `backend/app/seed_2026_coverage_docs.py`
- Test: `backend/tests/test_coverage_docs_seed.py`

**Interfaces:**
- Consumes: `app.services.kb_seed_common.get_or_create_doc_std`, `raw_text_is_grounded`
- Produces: `CoverageDocMap`, `DocRequirement` 행. `DocRequirement.anchor_phrase`는 `clause.text`의 부분 문자열.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_coverage_docs_seed.py`:

```python
"""청구서류 매핑과 서류 요건의 불변 조건."""
import sqlite3
from pathlib import Path

DB = Path(__file__).resolve().parents[1] / "data" / "app.db"


def _rows(sql):
    connection = sqlite3.connect(DB)
    try:
        return connection.execute(sql).fetchall()
    finally:
        connection.close()


def test_every_coverage_has_at_least_one_document():
    missing = _rows(
        """SELECT c.coverage_id, c.raw_name FROM coverage c
             LEFT JOIN coverage_doc_map m USING(coverage_id)
            WHERE m.coverage_doc_id IS NULL"""
    )
    assert missing == [], f"청구서류가 없는 담보: {missing}"


def test_doc_requirement_anchors_are_grounded():
    rows = _rows(
        """SELECT r.requirement_id, r.anchor_phrase, c.text
             FROM doc_requirement r JOIN clause c USING(clause_id)"""
    )
    assert rows, "doc_requirement가 비어 있다"
    broken = [rid for rid, anchor, text in rows if anchor.strip() not in text]
    assert broken == [], f"원문에 없는 앵커: {broken}"
```

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_coverage_docs_seed.py -v`
Expected: 두 테스트 모두 FAIL

- [ ] **Step 3~5:** 시드를 쓰고 실행해 통과시킨 뒤 커밋한다.

```bash
git add backend/app/seed_2026_coverage_docs.py backend/tests/test_coverage_docs_seed.py
git commit -m "2026년 담보별 청구서류와 서류 요건 재생성"
```

---

### Task 15: 중복 판정 규칙 재생성

**Files:**
- Create: `backend/app/seed_2026_overlap_rules.py`
- Modify: `backend/tests/test_overlap_rules_seed.py` (새 조항 기준으로 기대값 갱신)

**Interfaces:**
- Produces: `OverlapRule` 행. `anchor_phrase`는 대응 `clause.text`의 부분 문자열.

- [ ] **Step 1: 기존 테스트를 읽고 검증하려는 성질을 확인**

Run: `cd backend && cat tests/test_overlap_rules_seed.py`

기대값(조항 개수·특정 문구)은 새 원본 기준으로 고치고, 검증하려던 성질(앵커 grounding, 담보 쌍의 대칭성 등)은 유지한다.

- [ ] **Step 2: 실패를 확인**

Run: `cd backend && python -m pytest tests/test_overlap_rules_seed.py -v`
Expected: FAIL(구 조항 기준 기대값)

- [ ] **Step 3~5:** 시드를 쓰고 실행해 통과시킨 뒤 커밋한다.

```bash
git add backend/app/seed_2026_overlap_rules.py backend/tests/test_overlap_rules_seed.py
git commit -m "2026년 조항 기준 중복 판정 규칙 재생성"
```

---

### Task 16: 표준약관 대조 재생성

`standard_clause` 11행은 금융감독원 원문이라 그대로 남아 있다. 보험사 조항 쪽 대응만 다시 만든다.

**Files:**
- Create: `backend/app/seed_2026_clause_standard_map.py`
- Modify: `backend/tests/test_standard_terms_seed.py`

**Interfaces:**
- Produces: `ClauseStandardMap` 행. `relation`은 `SAME/BROADER/NARROWER/MISSING_IN_INSURER`. `MISSING_IN_INSURER`이면 `clause_id`와 `anchor_phrase_insurer`는 반드시 `None`.

- [ ] **Step 1: 실패하는 테스트를 확인**

Run: `cd backend && python -m pytest tests/test_standard_terms_seed.py -v`
Expected: FAIL(구 조항 id를 참조)

- [ ] **Step 2: 근거를 찾은 칸만 채운다**

대응 조항을 못 찾았거나 애매한 칸은 비워 둔다. 구판본에서는 54칸 중 9칸만 채웠다 — 근거 없이 단정하지 않는 것이 기존 방침이고 유지한다. 채운 칸과 비운 칸의 이유를 시드 docstring에 남긴다.

- [ ] **Step 3: MISSING_IN_INSURER 불변 조건 테스트를 추가**

```python
def test_missing_relation_has_no_insurer_anchor():
    connection = sqlite3.connect(DB)
    try:
        rows = connection.execute(
            """SELECT map_id, clause_id, anchor_phrase_insurer FROM clause_standard_map
                WHERE relation = 'MISSING_IN_INSURER'"""
        ).fetchall()
    finally:
        connection.close()
    for map_id, clause_id, anchor in rows:
        assert clause_id is None, f"map_id={map_id}: MISSING인데 clause_id가 있다"
        assert anchor is None, f"map_id={map_id}: MISSING인데 앵커가 있다"
```

- [ ] **Step 4: 실행하고 통과를 확인**

Run: `cd backend && python -m app.seed_2026_clause_standard_map && python -m pytest tests/test_standard_terms_seed.py -v`
Expected: 전부 PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/app/seed_2026_clause_standard_map.py backend/tests/test_standard_terms_seed.py
git commit -m "2026년 조항 기준 표준약관 대조 재생성"
```

---

### Task 17: 출처 등록부와 매니페스트 갱신

**Files:**
- Modify: `backend/data/dataset_manifest.json`
- Modify: `docs/compliance/source_register.md`
- Modify: `backend/PDF_EXTRACTION_PLAYBOOK.md` (파일 경로와 쪽수)
- Modify: `README.md` (데이터 현황)

**Interfaces:**
- Consumes: `data/raw_pdfs/source_files.json`의 해시, DB 실측 집계
- Produces: `verification_status='VERIFIED_ISSUED_FILE'`, `ranking_eligible=true`인 6개 source 항목

- [ ] **Step 1: 실측 집계를 뽑는다**

Run:
```bash
cd backend && python -c "
import sqlite3
connection = sqlite3.connect('data/app.db')
for row in connection.execute('''
  SELECT i.code,
         (SELECT COUNT(*) FROM coverage WHERE policy_version_id = pv.policy_version_id),
         (SELECT COUNT(*) FROM clause WHERE policy_version_id = pv.policy_version_id),
         (SELECT COUNT(*) FROM clause_incident_map m JOIN clause c USING(clause_id)
           WHERE c.policy_version_id = pv.policy_version_id),
         (SELECT COUNT(*) FROM clause_term t JOIN clause c USING(clause_id)
           WHERE c.policy_version_id = pv.policy_version_id),
         (SELECT COUNT(*) FROM coverage_doc_map d JOIN coverage cv USING(coverage_id)
           WHERE cv.policy_version_id = pv.policy_version_id)
    FROM policy_version pv JOIN product p USING(product_id) JOIN insurer i USING(insurer_id)
   ORDER BY i.code'''):
    print(row)
"
```

- [ ] **Step 2: 매니페스트를 다시 쓴다**

각 source 항목의 값:

```json
{
  "insurer": "SAMSUNG",
  "product_name": "해외여행보험",
  "policy_version": "2026수집본",
  "effective_date": null,
  "source_url": null,
  "source_type": "OFFICIAL_ISSUED_FILE",
  "downloaded_at": "2026-08-18",
  "sha256": "95fc1ac011933d8b329eeee3698cfd8adffa0540bf353b0e90dca446156406b7",
  "verification_status": "VERIFIED_ISSUED_FILE",
  "ranking_eligible": true,
  "coverage_count": 0,
  "clause_count": 0,
  "incident_map_count": 0,
  "term_count": 0,
  "doc_map_count": 0,
  "known_gap": "표지가 이미지라 약관 버전 코드와 시행일이 인쇄돼 있지 않다"
}
```

`*_count`는 Step 1의 실측값으로 채운다. `dataset_version`은 `travel-kb-2026.08.18-rebuild`, `verified_at`은 `2026-08-18`.

카카오 항목의 `known_gap`에 나머지 두 파일 해시를 적는다: `함께하는 해외여행보험(f558fafb…)·II(a4b41ae4…) 두 파일을 함께 읽어 통합했고 file_hash는 기준 파일인 해외여행보험(3ed801af…)의 것이다`.

- [ ] **Step 3: 등록부 표를 다시 쓴다**

`docs/compliance/source_register.md`의 검증 상태 정의 표에 한 행을 추가한다.

```markdown
| `VERIFIED_ISSUED_FILE` | 보험사가 공식 발행한 약관 파일(공개 URL 없음) | 허용 |
```

표준 출처 등록부 표를 6개사 새 값으로 바꾸고, "알려진 최신성 공백" 절을 다시 쓴다 — 메리츠·KB가 2차 출처/구버전이라 제외됐던 서술은 더 이상 사실이 아니다. 새 공백은 삼성·메리츠·KB·DB의 시행일 미인쇄다.

- [ ] **Step 4: 감사 스크립트 통과와 지문 동결**

Run:
```bash
cd backend && python -m scripts.validate_kb
python -m scripts.validate_kb --freeze
```
Expected: 오류 0. `effective_date`가 없는 4개사에 대한 경고는 남는다(사실이므로 정상).

- [ ] **Step 5: 커밋**

```bash
git add backend/data/dataset_manifest.json docs/compliance/source_register.md backend/PDF_EXTRACTION_PLAYBOOK.md README.md
git commit -m "2026년 약관 출처 등록부와 매니페스트 갱신"
```

---

### Task 18: 전체 회귀와 마무리

**Files:**
- Modify: 실패하는 테스트 파일들
- Delete: `backend/app/seed_samsung*.py` 등 구 시드 스크립트

- [ ] **Step 1: 구 시드 스크립트를 지운다**

새 시드로 대체됐고, 남겨두면 실행 시 구 조항이 되살아난다.

```bash
cd backend/app
git rm seed_samsung.py seed_samsung_full_chunk*.py seed_samsung_inj_deep.py seed_samsung_terms_docs.py \
       seed_hyundai.py seed_hyundai_full_chunk*.py seed_hyundai_inj_deep.py seed_hyundai_terms_docs.py \
       seed_meritz.py seed_meritz_full_*.py seed_meritz_inj_deep.py seed_meritz_terms_docs.py \
       seed_kb.py seed_kb_full_chunk*.py seed_kb_inj_deep.py seed_kb_terms_docs.py \
       seed_db.py seed_db_full_chunk*.py seed_db_inj_deep.py seed_db_terms_docs.py \
       seed_kakaopay.py seed_kakaopay_full_chunk*.py seed_kakaopay_inj_deep.py seed_kakaopay_terms_docs.py \
       seed_clause_incident_map.py seed_clause_standard_map.py seed_overlap_rules.py seed_personal_effects.py
```

`seed_db_full_gap_fill.py`처럼 위 패턴에 안 걸리는 파일이 남았는지 `ls seed_*.py`로 확인한다. 남기는 것: `seed_incident_types.py`, `seed_standard_clauses.py`, `seed_questions.py`, `seed_premiums.py`, `seed_travel_alerts.py`, `seed_country_language.py`, `seed_onsite_phrases.py`, `seed_simulation_scenarios.py`, `seed_validation_rules.py`, `seed_doc_requirements.py`, `seed_flight_delay_stats.py`, `seed_nonpayment_rates.py`, `seed_clause_term_doc*.py`는 조항을 참조하면 지운다.

- [ ] **Step 2: 구 시드를 부르는 곳이 없는지 확인**

Run: `cd backend && grep -rn "seed_samsung\|seed_hyundai\|seed_meritz\|seed_kb\b\|seed_db\b\|seed_kakaopay\|seed_personal_effects" --include=*.py . | grep -v "_2026"`
Expected: 결과 없음. 있으면 새 모듈명으로 고친다.

- [ ] **Step 3: 전체 테스트**

Run: `cd backend && python -m pytest -q`
Expected: 전부 PASS. 실패하는 테스트는 기대값을 새 원본 기준으로 고치고, 검증하려던 성질은 유지한다.

- [ ] **Step 4: 최종 검증 3종**

Run:
```bash
cd backend
python -m scripts.verify_clause_grounding
python -m scripts.validate_kb
python -m pytest -q
```
Expected: `grounding OK`, 감사 오류 0, 테스트 전부 통과.

- [ ] **Step 5: 커밋**

```bash
git add -A backend/app backend/tests
git commit -m "구 약관 시드 스크립트 제거와 회귀 테스트 갱신"
```

---

## 실행 순서 요약

Task 1 → 2 → 3 → 4 (판 갈기) → 5 (경계 확정) → 6~11 (정독, 보험사별 순차) → 12~16 (파생) → 17 (문서) → 18 (회귀).

Task 4는 되돌릴 수 없으므로 Task 1~3이 끝난 뒤에 실행한다. Task 3의 검증 스크립트가 먼저 있어야 Task 6부터 정독 결과를 즉시 확인할 수 있다.
